[pytest]
testpaths = tests
python_files = test_*.py
# La raíz del backend en sys.path: los tests importan `app.*` y `config`
# igual que la aplicación.
pythonpath = .
addopts = -q --tb=short
filterwarnings =
    ignore::DeprecationWarning:passlib.*

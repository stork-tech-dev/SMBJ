# --- Framework web ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Configuración ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Base de datos ---
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0

# --- Autenticación (módulo 02, se deja instalado desde el arranque) ---
pyjwt==2.10.1
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# --- Templates ---
jinja2==3.1.5

# --- Códigos de barras (módulo 04) ---
# Genera Code128 en SVG. El checksum módulo 103 (ISO/IEC 15417) lo
# calcula la librería; no se persiste. MIT, sin servicios externos.
python-barcode==0.15.1

# --- Reportes ---
weasyprint==63.1
openpyxl==3.1.5

# --- Utilidades ---
python-dateutil==2.9.0.post0

# --- Testing ---
pytest==8.3.4
httpx==0.28.1
